#include "framework/test_framework.h"
#include <railguard/utils/arrays.h>

TEST(Array) {
    ASSERT_TRUE(true);

}

TEST(Truc) {
    ASSERT_FALSE(false);
}

// Entry point for the tests
int main(void)
{
    RUN_ALL_TESTS();

    return 0;
}